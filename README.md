# Structural Arithmetic Modules

Verilog implementations and tests for structural arithmetic modules, including adders, multipliers, and dividers.

## Run the tests

Requirements: `iverilog` and `vvp`.

```bash
cd structural_arithmetic_modules
make test
```

Generated build files and waveforms are stored under `test/` and ignored by Git.
